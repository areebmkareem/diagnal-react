export const selectMovies = (state) => state?.movies?.currentMovies;

export const selectMovieInfo = (state) => state?.movies?.info;

export const selectSearchKeyword = (state) => state?.movies?.searchKey;
